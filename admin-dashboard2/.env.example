# Copy this file to `.env` and fill in values (see VERCEL-DEPLOY.md for Vercel).
# Do not use `.env.local` — the app uses `.env` only.
# Required: DATABASE_URL, ADMIN_JWT_SECRET, Firebase, optionally Clerk, GROQ, etc.
